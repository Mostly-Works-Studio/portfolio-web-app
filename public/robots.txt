# robots.txt for Mostly Works Studio Portfolio

# Allow all search engines to crawl the site
User-agent: *
Allow: /

# Sitemap location (update when sitemap is added)
Sitemap: https://panshul.dev/sitemap.xml

# Crawl-delay (optional, helps prevent server overload)
Crawl-delay: 1
